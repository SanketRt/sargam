# Core: what the solver and the local CLI need.
numpy>=1.26

# Hosted server only. The local tool (`sargam web`) runs on the standard
# library and does not need these.
fastapi>=0.115
uvicorn[standard]>=0.30
itsdangerous>=2.2
authlib>=1.3
# authlib's integration layer imports an http client internally and does
# not declare it. Without this the server imports fine until OAuth is
# configured, then dies on the first boot that takes that branch.
# httpx2 rather than httpx: authlib warns that the latter is deprecated.
httpx2>=0.28
cryptography>=42
# The model client itself. Without it a pasted key cannot be used, and
# the failure reads as a rejected key rather than a missing package.
anthropic>=0.40
